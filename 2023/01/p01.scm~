#lang racket

#;(define data (file->lines "input"))

(define data '("1abc2" "pqr3stu8vwx" "a1b2c3d4e5f" "treb7uchet"))

(define (process-line x)
  (string-length x))


(display (map process-line data))

#;(with (open-input-file "input")
      (display read-line))